[TOC]
# language syntax
AutoHotKey = coenraads.bracket-pair-colorizer
CMake      = twxs.cmake
proto      = zxh404.vscode-proto3

# text
Text    Marker         = ryu1kn.text-marker (高亮对象名)
Bracket Pair Colorizer = coenraads.bracket-pair-colorizer (彩色化成对括号)
Better  Align          = 文本的高等对齐 (需要手动触发)
Reopen Closed Tab      = uyiosa-enabulele.reopenclosedtab (重新打开)

# markdown
Markdown Preview Enhanced = shd101wyy.markdown-preview-enhanced

# c/c++
C/C++ GNU Global = jaycetyle.vscode-gnu-global (配合 global 插件)
Clang Format     = xaver.clang-format (格式化 c/c++)